{
  "name": "rogerai-web",
  "version": "0.0.0",
  "private": true,
  "description": "RogerAI static site - build the dist tree from web/src",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test --test-concurrency=1 test/*.mjs",
    "verify:artifacts": "node scripts/verify-artifacts.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
